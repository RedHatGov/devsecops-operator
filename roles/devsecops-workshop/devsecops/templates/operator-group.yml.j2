apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ operator_group_name | default(devsecops_namespace + '-operatorgroup') }}
  namespace: {{ devsecops_namespace }}
spec:
  targetNamespaces:
  - {{ devsecops_namespace }}
